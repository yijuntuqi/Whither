# =========================================================
# Whither 运行时依赖（Docker 部署专用）
# 版本对齐本地已验证可用的组合（2026-09-21）
# =========================================================

# LangChain 核心
langchain==1.4.0
langchain-core==1.6.3
langchain-community==0.4.2
langchain-classic==1.0.8
langchain-text-splitters==1.1.2

# Ollama（开发用本地模型）
langchain-ollama==1.0.1
ollama==0.6.2

# LangGraph / Agent 编排
langgraph==1.2.11
langgraph-prebuilt==1.1.0
langgraph-checkpoint==4.2.0
langgraph-checkpoint-postgres==3.0.5
langgraph-sdk==0.4.4

# MCP 集成（langchain-mcp-adapters 0.3.2 要求 langchain-core>=1.3.3, mcp>=1.24.0）
# 注：fastmcp 用于构建 MCP server，本项目只消费 MCP 服务，不需要
mcp==1.30.0
langchain-mcp-adapters==0.3.2

# LLM 供应商
openai==2.26.0
langchain-openai==1.1.11
dashscope==1.25.6

# 联网搜索
langchain-tavily==0.2.17

# Web 服务 / API / SSE
fastapi==0.135.1
uvicorn==0.46.0
sse-starlette==3.3.4
httpx==0.28.1
httpx-sse==0.4.3
aiohttp==3.12.14

# 配置管理
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.12.0
orjson==3.11.7

# 日志
loguru==0.7.3
tenacity==9.1.4

# PostgreSQL 检查点
psycopg[binary]==3.3.3
psycopg-pool==3.3.0

# SQLite 异步检查点（langgraph-checkpoint-sqlite 提供 AsyncSqliteSaver，aiosqlite 是驱动）
langgraph-checkpoint-sqlite==3.1.1
aiosqlite==0.22.1

# PDF 生成（Playwright + Chromium）
playwright==1.63.0

# PDF 解析
pymupdf==1.26.4

# RAG 向量计算
numpy==2.4.4
scipy==1.17.1
